---
phase: 02-heatmap-ats-dynamique
verified: 2026-05-14T00:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 2: Heatmap ATS Dynamique - Verification Report

**Phase Goal:** ats.html entierement pilote par weighted-topics.json - zero donnee statique dans le HTML
**Verified:** 2026-05-14
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Heatmap rows generated by JS from weighted-topics.json - zero topic names in body HTML | VERIFIED | body HTML has empty .heatmap div (line 244); topic.nom assigned only via textContent in script block |
| 2  | Year columns derived from union of all years in JSON data | VERIFIED | allYears = Array.from(new Set(yearsFlat)).sort() in loadData(); passed to buildYearHeader() which sets gridTemplateColumns |
| 3  | Cell states (hm-0 to hm-4) computed from topic.years and totalOccurrences | VERIFIED | hmClass from totalOccurrences (0=hm-0 to 4+=hm-4); presence via topic.years.indexOf(years[y]) !== -1 |
| 4  | Total column shows Nx count and proportional stabilo bar from topic.score / maxScore | VERIFIED | freqTotal.textContent = totalOccurrences + "x"; barFill.style.width = Math.round(score/maxSc*100)+"%" |
| 5  | Matiere chip click re-renders heatmap and priority list without page reload | VERIFIED | addEventListener on filterBarEl (line 404); handler calls buildYearHeader+renderHeatmapRows+renderPriorityList, no navigation |
| 6  | Topic priority list rendered from JSON sorted by score desc with rank badges | VERIFIED | topics.slice().sort((a,b)=>b.score-a.score); badge--critical/high/medium on i=0/1/2 |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| ats.html | Dynamic heatmap and filter tabs; fetch + createElement in script block | VERIFIED | fetch(./data/ats/poids/weighted-topics.json) at line 427; document.createElement used for every DOM element |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| fetch() | weighted-topics.json | async loadData() on page load | WIRED | Line 427: await fetch(./data/ats/poids/weighted-topics.json); loadData() invoked at line 468 |
| allYears | grid-template-columns | heatmapEl.style.gridTemplateColumns | WIRED | Line 279: gridTemplateColumns = "220px repeat("+years.length+", 1fr) 96px" |
| chip click | heatmap re-render | addEventListener on #filter-bar | WIRED | Line 404: filterBarEl.addEventListener("click",...); handler rebuilds heatmap and priority list |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| ats.html | mathTopics | data["mathematiques"] from JSON fetch | Yes - real weighted-topics.json | FLOWING |
| ats.html | allYears | Union of topic.years across all matieres | Yes | FLOWING |
| ats.html | maxScore | Math.max.apply over all topic.score values | Yes | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED - ats.html is a static HTML page with a fetch() data dependency; no runnable entry point without a dev server. Structural and wiring checks performed via grep and code inspection instead.

### Probe Execution

Step 7c: No probe scripts declared in PLAN or SUMMARY for this phase. No conventional probe-*.sh files found.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ATS-05 | 02-01-PLAN | Heatmap rows generated from JSON - zero static topic HTML | SATISFIED | Empty .heatmap div in body; rows built entirely in renderHeatmapRows() via createElement |
| ATS-06 | 02-01-PLAN | Year columns calculated dynamically from real data range | SATISFIED | allYears = Array.from(new Set(yearsFlat)).sort(); 7 years (2018-2024) from JSON |
| ATS-07 | 02-01-PLAN | Cell states computed from topic.years array | SATISFIED | present = topic.years.indexOf(years[y]) !== -1; hmClass from totalOccurrences |
| ATS-08 | 02-01-PLAN | Total column: Nx count + proportional bar from JSON score | SATISFIED | totalOccurrences+"x"; fill width = Math.round(score/maxSc*100)+"%" |
| ATS-09 | 02-01-PLAN | Matiere filter tabs click re-renders without page reload | SATISFIED | buildFilterTabs() generates chips from Object.keys(data); event delegation on #filter-bar re-renders heatmap and list |
| ATS-10 | 02-01-PLAN | Priority list sorted by score desc with rank badges | SATISFIED | renderPriorityList() sorts by b.score-a.score; badge--critical/high/medium on rank 0/1/2 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| ats.html | 374 | mastFill.style.width = "50%" | INFO | Intentional placeholder - mastery data is Phase 3+ scope per PLAN. Not a Phase 2 blocker. |
| ats.html | 378 | pctDiv.textContent = "--" | INFO | Intentional placeholder - mastery pct is Phase 3+ scope per PLAN. Not a Phase 2 blocker. |

No TBD, FIXME, or XXX debt markers found in ats.html.

The mastery fill (50%) and pct ("--") are explicitly documented as intentional in the PLAN:
"mastery data is Phase 3+ scope; 50% neutral placeholder is correct here per ROADMAP."
These are scoped deferrals, not Phase 2 gaps.

### Human Verification Required

None. All Phase 2 must-haves are verifiable programmatically via code inspection and grep analysis.

### Gaps Summary

No gaps. All 6 must-have truths are VERIFIED. All 6 requirements (ATS-05 through ATS-10) are SATISFIED.

The phase goal is fully achieved: ats.html is entirely driven by weighted-topics.json with zero
static topic/year/score data in the body HTML. Every dynamic element - year columns, heatmap rows,
cell state classes, Total column Nx counts and stabilo bars, matiere filter chips, and the priority
list with rank badges - is generated by JavaScript from the JSON data source at page load.

---
_Verified: 2026-05-14_
_Verifier: Claude (gsd-verifier)_
