---
phase: 02-heatmap-ats-dynamique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ats.html
autonomous: true
requirements:
  - ATS-05
  - ATS-06
  - ATS-07
  - ATS-08
  - ATS-09
  - ATS-10

must_haves:
  truths:
    - "Heatmap rows generated by JS from weighted-topics.json -- zero topic names in body HTML"
    - "Year columns derived from union of all years in JSON data"
    - "Cell states (hm-0 to hm-4) computed from topic.years.includes(year) and totalOccurrences"
    - "Total column shows Nx count and proportional stabilo bar from topic.score / maxScore"
    - "Matiere chip click re-renders heatmap and priority list without page reload"
    - "Topic priority list rendered from JSON sorted by score desc with rank badges"
  artifacts:
    - path: "ats.html"
      provides: "Dynamic heatmap and filter tabs"
      contains: "fetch weighted-topics.json and document.createElement in script block"
  key_links:
    - from: "fetch()" to: "weighted-topics.json" via: "async loadData() on page load"
    - from: "allYears" to: "grid-template-columns" via: "heatmapEl.style.gridTemplateColumns"
    - from: "chip click" to: "heatmap re-render" via: "addEventListener on #filter-bar"
---

<objective>
Replace all static hardcoded data in ats.html with JavaScript that fetches weighted-topics.json
and renders the heatmap rows, year columns, cell states, Total column, matiere filter tabs, and
topic priority list dynamically.

Purpose: ATS-05 through ATS-10 -- the heatmap must reflect real concours data, not a static mockup.
Output: ats.html with zero hardcoded topic/year/score data; all content generated from
weighted-topics.json at page load.
</objective>

<execution_context>
@C:/Users/Administrateur/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrateur/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

CRITICAL -- file writing constraint:
The gsd-workflow-guard.js hook blocks the Write and Edit tools in Claude Code.
ALL file creation and modification MUST be done via PowerShell WriteAllText:

  [System.IO.File]::WriteAllText(
    "C:\Users\Administrateur\Documents\projetPrepaV2\ats.html",
    $content,
    [System.Text.UTF8Encoding]::new($false)
  )

Do NOT use the Write tool or Edit tool for ats.html. Build the full file content as a PowerShell
variable and write it in one call.
Do NOT use backtick characters (`) anywhere in PowerShell-embedded JS -- use string concatenation
with + instead of template literals throughout.
</context>

<interfaces>
Data shape from data/ats/poids/weighted-topics.json:

Top-level keys: _meta, mathematiques, physique-chimie, francais, anglais, sciences-de-lingenieur

Each matiere value is an array of topic objects:
  id: string               -- "analyse", "algebre-lineaire"
  nom: string              -- "Analyse", "Algebre lineaire"
  totalOccurrences: number -- e.g. 14 (Analyse), 10 (Algebre lineaire), 3 (Geometrie), 1 (Calcul)
  score: number            -- e.g. 8.611 (used for proportional bar width)
  years: number[]          -- e.g. [2018, 2019, 2020, 2021, 2022, 2023, 2024]

Topics within each matiere are already sorted by score descending.
Empty matieres (physique-chimie, francais, anglais, sciences-de-lingenieur) have value [].

CSS classes already defined in ats.html style block (do not add new CSS for these):
  .hm-0 .hm-1 .hm-2 .hm-3 .hm-4  -- frequency shading (hm-0 = grey/absent, hm-4 = darkest)
  .heatmap       -- grid container; grid-template-columns MUST be set by JS
  .col-head      -- year header cell
  .row-head      -- topic name cell (font-weight 700)
  .hm-cell       -- individual year cell (height 36px, border, border-radius 6px)
  .row-end       -- Total column container (flex column, align-items flex-end)
  .freq-total    -- "Nx" count text (22px Archivo Black, font-weight 900)
  .freq-bar .fill -- proportional stabilo bar (height 5px, fill background = stabilo yellow)
  .filter-bar .chip -- matiere tab; class "active" = selected chip (dark bg)
  .topic-list .topic-row -- priority list container and row
  .row-badge .badge--critical .badge--high .badge--medium -- priority badges
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build dynamic year columns and heatmap grid structure</name>

  <read_first>
    - ats.html (full file -- understand current static structure before rewriting)
    - data/ats/poids/weighted-topics.json (examine years arrays across all matieres)
  </read_first>

  <action>
    Rewrite the entire ats.html file via PowerShell WriteAllText. The rewritten file must:

    1. Have an empty .heatmap div in the body HTML (no child elements hardcoded).
       Remove the static col-head divs and all hm-cell, row-head, row-end divs from the body.

    2. In the script block, implement buildYearHeader(heatmapEl, allYears):
       - Set heatmapEl.style.gridTemplateColumns using string concatenation:
         "220px repeat(" + allYears.length + ", 1fr) 96px"
       - Set heatmapEl.innerHTML = ""
       - Append: one empty div (spacer for row-head column),
         then for each year one div with class "col-head" and text = the year value,
         then one div with class "col-head" and text "Total"
       - Use document.createElement for every element. No innerHTML insertion.

    3. In loadData() after fetch("./data/ats/poids/weighted-topics.json") and JSON parse:
       - Compute allYears: for each matiere key (skip _meta), for each topic, push all
         topic.years values. Then: allYears = Array.from(new Set(flat)).sort()
       - Declare allYears at module level (var before loadData function)
       - Declare heatmapEl = document.querySelector(".heatmap") at module level
       - Call buildYearHeader(heatmapEl, allYears)

    Preserve unchanged: all CSS in the style block, sidebar HTML, topbar HTML, hero stats HTML,
    card-head legend, IA strategic card. Only .heatmap div interior and script block change.

    File write: build the full HTML as a JS string in a Node script or as a PowerShell variable,
    then write via PowerShell:
      powershell.exe -Command "[System.IO.File]::WriteAllText(path, content, [System.Text.UTF8Encoding]::new($false))"
    No backtick characters anywhere in the embedded JS code.
  </action>

  <verify>
    Run: node scripts/screenshot.js
    Screenshot shows year column headers rendered from data.
    Check: the .heatmap div in the written ats.html file has no child elements between its tags.
    Check: no static year label strings appear in body HTML outside the script tag.
  </verify>

  <acceptance_criteria>
    - .heatmap div in body HTML is empty (no child elements hardcoded between the tags)
    - Script block contains "buildYearHeader" function definition
    - Script block contains "gridTemplateColumns" (setting grid columns dynamically)
    - Script block contains "fetch" and "weighted-topics.json"
    - Script block contains "document.createElement"
    - Script block contains "allYears" variable
    - No static year labels appear in body HTML outside the script tag
    - File written with no BOM (PowerShell UTF8Encoding false)
  </acceptance_criteria>

  <done>Year columns render from the actual years in weighted-topics.json. Grid column count adapts
  to the data. No year values exist as static HTML in the body.</done>
</task>

<task type="auto">
  <name>Task 2: Generate heatmap rows, cell states, and Total column from JSON</name>

  <read_first>
    - ats.html (as modified by Task 1 -- see script block structure to extend)
    - data/ats/poids/weighted-topics.json (confirm fields: id, nom, totalOccurrences, score, years)
  </read_first>

  <action>
    Extend the ats.html script block (full file rewrite via PowerShell WriteAllText) to add
    renderHeatmapRows(heatmapEl, topics, allYears, maxScore, matiereSlug).

    For each topic in the topics array:

    1. Compute hmClass from topic.totalOccurrences:
         0 -> "hm-0"
         1 -> "hm-1"
         2 -> "hm-2"
         3 -> "hm-3"
         4 or more -> "hm-4"

    2. Create a wrapper div: class "hm-row", style.display = "contents",
       wrapper.setAttribute("data-matiere", matiereSlug).
       Append wrapper to heatmapEl.
       (display:contents makes the wrapper invisible to CSS grid; children flow into grid directly.)

    3. Append children to the wrapper:
       a. Row-head: div class "row-head", textContent = topic.nom
       b. For each year in allYears:
            var present = topic.years.indexOf(year) !== -1
            If present: div class "hm-cell " + hmClass, textContent = "1"
            If absent:  div class "hm-cell hm-0", textContent = "--"
       c. Row-end div class "row-end" containing:
            span class "freq-total", textContent = topic.totalOccurrences + "x"
            div class "freq-bar" with child div class "fill",
              fill.style.width = Math.round((topic.score / maxScore) * 100) + "%"

    Compute maxScore before calling renderHeatmapRows: flatten all matiere arrays from data
    (exclude _meta key), map each topic to topic.score, take Math.max.apply(null, scores).
    Declare maxScore as a module-level variable set inside loadData().

    After defining renderHeatmapRows, call it inside loadData() for the default matiere:
      renderHeatmapRows(heatmapEl, data["mathematiques"], allYears, maxScore, "mathematiques")

    Full file rewrite via PowerShell WriteAllText with [System.Text.UTF8Encoding]::new($false).
    No backtick characters in JS code.
  </action>

  <verify>
    Run: node scripts/screenshot.js
    Screenshot must show 4 heatmap rows: Analyse, Algebre lineaire, Geometrie, Calcul differentiel.
    Total column: Analyse = "14x" with 100% bar width. Algebre lineaire = "10x" with ~71% bar.
    Check ats.html: topic names must not appear in body HTML outside the script tag.
  </verify>

  <acceptance_criteria>
    - Body HTML contains zero occurrences of hardcoded topic names outside the script tag
      (Analyse, Algebre, Geometrie, Probabilites, Integration, Topologie, Calcul all absent from body)
    - Script block contains "renderHeatmapRows" function definition
    - Script block contains "totalOccurrences"
    - Script block contains "freq-total"
    - Script block contains "freq-bar"
    - Script block contains "hm-cell"
    - Script block contains "maxScore"
    - Script block contains "data-matiere" (set on wrapper divs)
    - Wrapper divs use style.display = "contents" to preserve grid flow
    - Bar width for Analyse = 100% (it has the highest score = maxScore)
    - Bar width for Algebre lineaire approximately 71% (6.14 / 8.61 * 100)
  </acceptance_criteria>

  <done>Heatmap rows, cell states, and Total column fully generated from weighted-topics.json.
  No topic name, occurrence count, or score value appears as static HTML.
  Total bar is proportional to topic.score relative to maxScore across all matieres.</done>
</task>

<task type="auto">
  <name>Task 3: Wire matiere filter tabs and render topic priority list</name>

  <read_first>
    - ats.html (as modified by Task 2 -- locate filter-bar div and topic-list div in body)
    - data/ats/poids/weighted-topics.json (confirm matiere keys; note which have empty arrays)
  </read_first>

  <action>
    Extend ats.html (full file rewrite via PowerShell WriteAllText) to complete dynamic behavior.

    A. MATIERE FILTER TABS (ATS-09):

    In body HTML: replace the static chip spans in .filter-bar with an empty div
    id="filter-bar" class="filter-bar" (no child elements hardcoded).

    In script block, add buildFilterTabs(data):
    - Define MATIERE_LABELS as a plain object using property assignment (no backticks):
        var MATIERE_LABELS = {};
        MATIERE_LABELS["mathematiques"] = "Maths";
        MATIERE_LABELS["physique-chimie"] = "Physique-Chimie";
        MATIERE_LABELS["francais"] = "Francais";
        MATIERE_LABELS["anglais"] = "Anglais";
        MATIERE_LABELS["sciences-de-lingenieur"] = "SI";
    - Get the matiere keys: Object.keys(data).filter(function(k){ return k !== "_meta"; })
    - For each slug: create span with class "chip", set span.dataset.matiere = slug,
      span.textContent = MATIERE_LABELS[slug] || slug.
      Add class "active" to the mathematiques chip.
      Append to document.getElementById("filter-bar").

    Add click event on document.getElementById("filter-bar") (event delegation):
      filterBarEl.addEventListener("click", function(e) {
        var chip = e.target.closest(".chip"); if (!chip) return;
        document.querySelectorAll(".chip").forEach(function(c){ c.classList.remove("active"); });
        chip.classList.add("active");
        var slug = chip.dataset.matiere;
        var topics = data[slug] || [];
        heatmapEl.innerHTML = "";
        buildYearHeader(heatmapEl, allYears);
        if (topics.length === 0) {
          var msg = document.createElement("div");
          msg.className = "row-head";
          msg.textContent = "Aucun sujet tague pour cette matiere.";
          heatmapEl.appendChild(msg);
        } else {
          renderHeatmapRows(heatmapEl, topics, allYears, maxScore, slug);
        }
        renderPriorityList(topics);
      });

    B. TOPIC PRIORITY LIST (ATS-10):

    In body HTML: replace the static .topic-list div (with all hardcoded .topic-row children)
    with an empty div id="topic-list" class="topic-list".

    In script block, add renderPriorityList(topics):
    - var listEl = document.getElementById("topic-list"); listEl.innerHTML = "";
    - If topics.length === 0:
        var empty = document.createElement("div"); empty.className = "topic-row";
        empty.textContent = "Aucune donnee pour cette matiere.";
        listEl.appendChild(empty); return;
    - For each topic at index i:
        var row = document.createElement("div"); row.className = "topic-row";
        Name div: class "name", textContent = topic.nom.
          If i === 0: append span class "row-badge badge--critical" text "Critique"
          If i === 1: append span class "row-badge badge--high" text "Haute"
          If i === 2: append span class "row-badge badge--medium" text "Moyen"
          (i >= 3: no badge)
        Freq div: class "freq",
          textContent = topic.totalOccurrences + "x / " + topic.years.length + " ans"
        Mastery div: class "mastery" with child div class "fill", fill.style.width = "50%"
          (mastery data is Phase 3+ scope; 50% neutral placeholder is correct here per ROADMAP)
        Pct div: class "pct", textContent = "--"
        Append all four children to row. Append row to listEl.

    Call renderPriorityList and buildFilterTabs inside loadData() after renderHeatmapRows.

    Full file rewrite via PowerShell WriteAllText with [System.Text.UTF8Encoding]::new($false).
    No backtick characters in JS.
  </action>

  <verify>
    Run: node scripts/screenshot.js
    Screenshot must show:
    - Filter bar: 5 chips (Maths, Physique-Chimie, Francais, Anglais, SI) with Maths chip active
    - Priority list: 4 rows -- Analyse (Critique badge), Algebre lineaire (Haute badge),
      Geometrie (Moyen badge), Calcul differentiel (no badge)
    - Freq text for Analyse: "14x / 7 ans"

    Manual browser test (serve via: node scripts/_server.cjs, open localhost:8080/ats.html):
    - Click Physique-Chimie chip: heatmap shows "Aucun sujet tague" message
    - Click Maths chip: heatmap returns to 4 rows, priority list returns to 4 rows
    - No page reload occurs on any chip click
  </verify>

  <acceptance_criteria>
    - Body HTML: #filter-bar div is empty (no static chip spans hardcoded)
    - Body HTML: #topic-list div is empty (no static topic-row divs hardcoded)
    - Script block contains "buildFilterTabs" function definition
    - Script block contains "renderPriorityList" function definition
    - Script block contains "MATIERE_LABELS"
    - Script block contains "dataset.matiere" (chip attribute access)
    - Script block contains "badge--critical"
    - Script block contains "badge--high"
    - Script block contains "badge--medium"
    - Script block contains "Aucun sujet" (empty matiere guard message)
    - Script block contains "addEventListener" on filter-bar element
    - No static text "Critique", "Haute", "Moyen", "Maths", "Physique" appears in body HTML outside script tag
  </acceptance_criteria>

  <done>
    All 6 requirements delivered:
    ATS-05: heatmap rows generated in JS from JSON -- zero static HTML topic data
    ATS-06: year columns computed dynamically from union of all topic.years arrays
    ATS-07: cell states (hm-0 to hm-4) derived from topic.years presence and totalOccurrences
    ATS-08: Total column shows totalOccurrences + stabilo bar proportional to topic.score / maxScore
    ATS-09: matiere chips generated from JSON keys; click re-renders heatmap and priority list
    ATS-10: priority list rendered under heatmap sorted by score desc with rank-based badges
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetch() to JSON file | Static JSON served locally -- no user input, no external network call |
| User click to DOM state | Chip clicks mutate DOM only -- no server round-trip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | weighted-topics.json | accept | Local static file, no auth, no PII, single-user dev tool |
| T-02-02 | Information Disclosure | fetch() error path | mitigate | Wrap fetch in try/catch; on error show "Donnees non disponibles" div in heatmap instead of silent crash |
| T-02-03 | Denial of Service | Malformed JSON | mitigate | Guard: if (!Array.isArray(topics)) treat as []; show empty-matiere message instead of crashing |
| T-02-04 | XSS via nom field | textContent assignment | accept | All topic names come from local JSON generated by compute-weights.js; textContent (not innerHTML) prevents injection regardless |
</threat_model>

<verification>
End-to-end check after all 3 tasks complete:

1. Grep gates -- these strings must NOT appear in body HTML outside the script tag:
   - "Analyse" (topic name)
   - "col-head" (class -- only in createElement calls in script)
   - "freq-total" (class -- only in createElement calls)
   - "topic-row" (class -- only in createElement calls)
   - "Critique" (badge text -- only in createElement calls)

2. Column count: heatmap shows 7 year columns (2018-2024) matching the current data.

3. Filter switching: click each of the 5 chips -- both heatmap and priority list update.
   Physique-Chimie shows "Aucun sujet tague" message. No page reload on any click.

4. Playwright screenshot confirms:
   - 4 topic rows for mathematiques
   - Analyse: Total "14x", bar at 100%
   - Algebre lineaire: Total "10x", bar at approximately 71%
   - 5 filter chips visible, Maths chip active (dark background)
   - 4 priority rows with badges: Critique, Haute, Moyen, no badge
</verification>

<success_criteria>
- ats.html fetches weighted-topics.json on load and renders all heatmap content from it
- Zero topic names, year values, occurrence counts, or score values exist as static HTML in the body
- Heatmap grid column count matches actual year range in data (currently 7: 2018-2024)
- Cell class (hm-0 through hm-4) computed from topic.totalOccurrences; presence from topic.years check
- Total column displays totalOccurrences + stabilo bar proportional to topic.score / maxScore
- All 5 matiere chips generated from JSON keys; clicking any chip re-renders heatmap and priority list
- Topic priority list shows all topics for active matiere sorted by score desc with rank badges
- All 6 requirements (ATS-05 through ATS-10) verified as done
</success_criteria>

<output>
After completion, create .planning/phases/02-heatmap-ats-dynamique/02-01-SUMMARY.md
</output>
