# Skills — mémo

Référence à charger uniquement quand pertinent. L'essentiel (GSD, `/simplify`, `/review`, contraintes) est dans `CLAUDE.md`.

## Avant feature non-triviale
- **UI/visuel** → `/plan-design-review` (lock design avant code).
- **Logique/archi** → `/plan-eng-review` (data flow, edge cases).
- Feature simple : skip, code directement.

## Skills à fréquence rare

| Skill | Quand |
|---|---|
| `/investigate` | Bug → trouve cause racine, pas un patch. Hypothèse explicite obligatoire. |
| `/codex` | Second avis indépendant avant un gros choix (mode challenge ou review). |
| `/cso` | Audit sécurité complet (mensuel). |
| `/retro` | Bilan progression (hebdo/quinzaine). |
| `/benchmark` | Avant/après refactor perf. |
| `/canary` | Surveillance post-déploiement. |
| `/health` | Score global qualité code. |

## Mise à jour
Si une règle bloque ou si un meilleur workflow émerge, mettre à jour ici — pas dans la mémoire Claude (qui ne traverse pas les conversations).
