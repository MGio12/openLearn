# Phase 1 — ATS Cockpit Fonctionnel

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rendre `ats.html` fonctionnel avec les vraies données JSON, corriger la visualisation des fréquences (totaux visuellement dominants), et mettre en place la boucle Playwright de validation visuelle.

**Architecture:** `ats.html` charge les données via `fetch()` depuis `data/ats/poids/weighted-topics.json` (généré par `scripts/compute-weights.js`). Le script lit tous les fichiers `data/ats/sujets/**/*.json` et calcule les scores pondérés. Playwright prend des screenshots que Claude lit pour valider l'UI.

**Tech Stack:** HTML/CSS/JS vanilla, Node.js (script de calcul), Playwright (screenshots), Codex (implémentation code), Claude (orchestration + review visuel)

**Rôles:**
- Codex — écrit tout le code (HTML, CSS, JS, Node scripts)
- Claude — orchestre, lit les PDFs pour le tagging, review les screenshots Playwright

---

### Task 1 : Script `compute-weights.js`

**Rôle : Codex**

**Files:**
- Create: `scripts/compute-weights.js`
- Modify: `data/ats/poids/weighted-topics.json` (output généré)

- [ ] **Step 1 : Créer `scripts/compute-weights.js`**

```js
// scripts/compute-weights.js
// Usage: node scripts/compute-weights.js
// Reads data/ats/sujets/**/*.json -> writes data/ats/poids/weighted-topics.json

import { readdirSync, readFileSync, writeFileSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = join(__dirname, '..');
const SUJETS_DIR = join(ROOT, 'data', 'ats', 'sujets');
const OUTPUT = join(ROOT, 'data', 'ats', 'poids', 'weighted-topics.json');

const RECENCY_DECAY = 0.9;
const JURY_BONUS = 0.2;
const CURRENT_YEAR = new Date().getFullYear();

const MATIERES = ['mathematiques', 'physique-chimie', 'francais', 'anglais', 'sciences-de-lingenieur'];

function computeWeights() {
  const result = {};

  for (const matiere of MATIERES) {
    const topicMap = {};
    const years = readdirSync(SUJETS_DIR).filter(d => /^\d{4}$/.test(d)).sort();

    for (const year of years) {
      const filePath = join(SUJETS_DIR, year, `${matiere}.json`);
      let data;
      try { data = JSON.parse(readFileSync(filePath, 'utf8')); }
      catch { continue; }

      const age = CURRENT_YEAR - parseInt(year);
      const recencyWeight = Math.pow(RECENCY_DECAY, age);

      for (const topic of (data.topics || [])) {
        if (!topicMap[topic.id]) {
          topicMap[topic.id] = { id: topic.id, nom: topic.nom, totalOccurrences: 0, score: 0, years: [] };
        }
        topicMap[topic.id].totalOccurrences += topic.occurrences;
        topicMap[topic.id].score += topic.occurrences * recencyWeight;
        if (topic.mentionJury) topicMap[topic.id].score += JURY_BONUS;
        topicMap[topic.id].years.push(parseInt(year));
      }
    }

    result[matiere] = Object.values(topicMap).sort((a, b) => b.score - a.score);
  }

  const output = {
    _meta: {
      generated: new Date().toISOString(),
      formula: 'score = frequency x recencyWeight(0.9^age) + juryBonus(0.2)',
      recencyDecay: RECENCY_DECAY,
      juryBonus: JURY_BONUS,
      lastUpdated: new Date().toISOString()
    },
    ...result
  };

  writeFileSync(OUTPUT, JSON.stringify(output, null, 2), 'utf8');
  console.log(`weighted-topics.json genere — ${Object.keys(result).length} matieres`);
  for (const [m, topics] of Object.entries(result)) {
    console.log(`   ${m}: ${topics.length} topics`);
  }
}

computeWeights();
```

- [ ] **Step 2 : Créer `package.json`**

```json
{
  "name": "outil-prepa",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "weights": "node scripts/compute-weights.js",
    "screenshot": "node scripts/screenshot.js"
  }
}
```

- [ ] **Step 3 : Exécuter**

```
node scripts/compute-weights.js
```

Expected:
```
weighted-topics.json genere — 5 matieres
   mathematiques: 0 topics
   physique-chimie: 0 topics
   francais: 0 topics
   anglais: 0 topics
   sciences-de-lingenieur: 0 topics
```
(Normal — les sujets sont encore vides)

---

### Task 2 : Script Playwright `screenshot.js`

**Rôle : Codex**

**Files:**
- Create: `scripts/screenshot.js`
- Create: `.gitignore` (ajouter screenshots/ et node_modules/)

- [ ] **Step 1 : Installer Playwright**

```
npm install --save-dev playwright
npx playwright install chromium
```

- [ ] **Step 2 : Créer `scripts/screenshot.js`**

```js
// scripts/screenshot.js
// Usage: node scripts/screenshot.js [url] [name]
// Output: screenshots/{name}-full.png et screenshots/{name}-fold.png

import { chromium } from 'playwright';
import { mkdirSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = join(__dirname, '..');
const OUT_DIR = join(ROOT, 'screenshots');
mkdirSync(OUT_DIR, { recursive: true });

const url = process.argv[2] || 'http://127.0.0.1:5500/ats.html';
const name = process.argv[3] || 'ats';

const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1440, height: 900 } });
await page.goto(url, { waitUntil: 'networkidle' });

await page.screenshot({ path: join(OUT_DIR, `${name}-full.png`), fullPage: true });
await page.screenshot({ path: join(OUT_DIR, `${name}-fold.png`), fullPage: false });

await browser.close();
console.log(`Screenshots sauvegardes dans screenshots/${name}-*.png`);
```

- [ ] **Step 3 : Créer `.gitignore`**

```
screenshots/
node_modules/
```

- [ ] **Step 4 : Tester** (Live Server doit tourner sur port 5500)

```
node scripts/screenshot.js http://127.0.0.1:5500/ats.html ats-baseline
```

Expected: `screenshots/ats-baseline-full.png` et `screenshots/ats-baseline-fold.png` créés.

---

### Task 3 : Correction visuelle fréquences dans `ats.html`

**Rôle : Codex implémente, Claude review**

**Problème :** La colonne "Total" de la heatmap est du texte mono 12px. Elle doit être visuellement dominante — c'est la donnée stratégique principale.

**Files:**
- Modify: `ats.html` (styles inline + HTML heatmap)

- [ ] **Step 1 : Ajouter ces styles dans le `<style>` inline de `ats.html`**

```css
.heatmap .row-end {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 4px;
  min-width: 72px;
  padding-left: 16px;
}
.freq-total {
  font-family: var(--font-display);
  font-size: 22px;
  line-height: 1;
  letter-spacing: -.01em;
  color: var(--ink);
  font-weight: 900;
}
.freq-label {
  font-size: 10px;
  color: var(--ink-3);
  letter-spacing: .05em;
  text-transform: uppercase;
}
.freq-bar {
  width: 100%;
  height: 5px;
  background: var(--paper-2);
  border-radius: 999px;
  overflow: hidden;
}
.freq-bar .fill {
  height: 100%;
  background: var(--stabilo);
  border-radius: 999px;
}
```

- [ ] **Step 2 : Remplacer chaque `.row-end` dans la heatmap**

Avant (exemple) :
```html
<div class="row-end">9x</div>
```

Après (le max est 9, calculer width% = occurrences/max*100) :
```html
<div class="row-end">
  <span class="freq-total">9x</span>
  <div class="freq-bar"><div class="fill" style="width:100%"></div></div>
</div>
```

Appliquer pour chaque ligne avec le bon pourcentage (6x = 67%, 5x = 56%, 4x = 44%, 3x = 33%, 2x = 22%, 1x = 11%).

- [ ] **Step 3 : Screenshot baseline**

```
node scripts/screenshot.js http://127.0.0.1:5500/ats.html ats-freq-v1
```

- [ ] **Step 4 : Claude review `screenshots/ats-freq-v1-fold.png`**

Claude évalue 4 critères :
1. Hiérarchie visuelle — le total domine-t-il la ligne ?
2. Cohérence design — couleurs et typo dans le système ?
3. Lisibilité — colonne ni trop large ni trop étroite ?
4. Promesse produit — sentiment "données stratégiques claires" ?

PASS (4/4) -> Task 4. FIX -> Codex corrige -> retour Step 3.

- [ ] **Step 5 : Commit si PASS**

```
git add ats.html
git commit -m "fix: heatmap totaux visuellement dominants — Archivo Black 22px + barre stabilo"
```

---

### Task 4 : Wiring `ats.html` vers les données JSON réelles

**Rôle : Codex**

**Files:**
- Modify: `ats.html` (ajouter script fetch en bas du body)

- [ ] **Step 1 : Ajouter avant `</body>` dans `ats.html`**

```html
<script type="module">
const MATIERE = 'mathematiques';

async function loadData() {
  try {
    const res = await fetch('./data/ats/poids/weighted-topics.json');
    const data = await res.json();
    return data[MATIERE] || [];
  } catch (e) {
    console.warn('weighted-topics.json non accessible:', e.message);
    return [];
  }
}

function renderTopicList(topics) {
  if (!topics.length) {
    console.log('Aucun topic charge (sujets pas encore tagues — normal en Phase 1)');
    return;
  }
  console.log(`${topics.length} topics charges pour ${MATIERE}`);
  // Phase 2 : rendu dynamique de la heatmap et de la priority list
}

loadData().then(renderTopicList);
</script>
```

- [ ] **Step 2 : Vérifier dans la console navigateur**

`http://127.0.0.1:5500/ats.html` -> Console :
- Aucune erreur 404
- Log `Aucun topic charge (sujets pas encore tagues — normal en Phase 1)`

- [ ] **Step 3 : Commit**

```
git add ats.html
git commit -m "feat: ats.html wired to weighted-topics.json via fetch() — pret pour donnees reelles"
```

---

### Task 5 : Tagging sujets maths 2024 + 2023

**Rôle : Claude lit les PDFs, remplit les JSON**

**Files:**
- Modify: `data/ats/sujets/2024/mathematiques.json`
- Modify: `data/ats/sujets/2023/mathematiques.json`

- [ ] **Step 1 : Claude fetch le PDF 2024**

URL : `https://www.concoursats.fr/pdfs/ats/annales/2024/Maths_ATS_956_2024.pdf`

Identifier les topics selon les IDs de `INSTRUCTIONS_TAGGING.md` et remplir le JSON.

- [ ] **Step 2 : Remplir `data/ats/sujets/2024/mathematiques.json`**

Format :
```json
{
  "annee": 2024,
  "matiere": "mathematiques",
  "source": "Arts et Metiers",
  "url": "https://www.concoursats.fr/pdfs/ats/annales/2024/Maths_ATS_956_2024.pdf",
  "topics": [
    {
      "id": "algebre-lineaire",
      "nom": "Algebre lineaire",
      "sousChapitre": "Valeurs propres et vecteurs propres",
      "occurrences": 1,
      "mentionJury": false,
      "notes": ""
    }
  ]
}
```

- [ ] **Step 3 : Meme chose pour 2023**

- [ ] **Step 4 : Regenerer weighted-topics.json**

```
node scripts/compute-weights.js
```

Expected : `mathematiques: N topics` (N > 0).

- [ ] **Step 5 : Verifier console navigateur**

Log attendu : `N topics charges pour mathematiques`.

- [ ] **Step 6 : Commit**

```
git add data/ats/sujets/2024/mathematiques.json data/ats/sujets/2023/mathematiques.json data/ats/poids/weighted-topics.json
git commit -m "data: tag sujets ATS maths 2024 + 2023 — premiers topics reels"
```
